# 트리니티(CPO) 보고 — 2026-07-10 p1/p2/p3 미감 개선

## 무엇을 / 왜
Sovereign "p1/p2/p3 항상 보정·개선" 상시명 + 센스 스킬 전군 이식 기반. 유저관점 짜침·깨짐 제거.

### p1 다이달로스 (~/daedalus-conquest — 그록 충돌 없음)
1. **연령게이트 CTA 거대블록 → 골드 알약**: 전역 `button{flex:1}`이 flex-column 게이트 버튼을 반화면 스트레치. `#age-gate button{flex:0 0 auto}` 추가. (style.css)
2. **히어로 워터마크 제거**: `#heroes::before` 6.5px "군단 지휘 본부…" 초상화와 겹쳐 깨진 글자. 제거(절제). (style.css:592)
3. **초록 유도힌트 재설계**: raw #22c55e z:9999(odds모달 위 부유, 디버그버튼 인상) → 테마 골드 그라디언트 + z:11(승리오버레이 위 유도, 타 모달 아래로). (game.js:2959)

### p3 Aria (~/p3-companion — 소유권: UI=트리니티)
4. **언어 자동감지**: TG language_code→나라별 자동(없으면 en). 인라인·자립형. (script.js load)
5. **폴백 아바타 프리미엄 주얼톤**: 사진없는 90명 무디초록 뭉침 → 밤/관능 무드 팔레트(각자 다름). (script.js+style.css)
6. 샵 프리미엄 카드 재디자인+i18n+SFW (앞 세션).

### 조율
- 그록에 p3 소유권 분담 핸드오프(UI=트리니티/데이터=그록) — 동시편집 레이스 차단.

## 검증(측정치)
- p1: 연령게이트 골드알약 렌더 확인(스샷). game.js/style.css node --check ✅.
- p3: script.js node --check ✅. 아바타 그리드 렌더(색·모노그램·이모지 전부 다름) 확인. 자동감지 로직 정직검증(실TG서 작동, 로컬은 SDK가 목업 덮어 검증불가).

## 다음 한 걸음
- p1 P1: 언어혼재 i18n 누락키 정리(#shop-gacha/#gacha-rates), SSR연출 시퀀스 분리.
- p2 audit 핸드오프 기반 개선.
- p3: 페르소나 이름 라틴 기본화(그록 데이터 조율 후).

## ⚠️ 승인 필요
- 전부 워킹트리 수정(되돌림가능·미커밋). **git push/배포는 Sovereign y/n.**
- p1·p3 라이브 반영하려면 승인 필요(현재 로컬만).
