# 📋 Morpheus 보고 — p1~p21 전수 점검 (2026-07-16)

## 무엇을
- **전 프로젝트 지형맵 + JS 신택스 헬스 스윕** (p1~p21, 21개).
- **p1(daedalus) 배포게이트 verify.sh** 실행 → 진짜 버그 1건 발견·수정.
- **24/7 launchd 인프라** 구축(별건, com.legion.daily).

## 왜
- neo 지시 "p1~p21 점검 후 해야 할 것 하라."
- 계측 계약 위반 = North Star 이벤트가 라이브에서 조용히 폐기되는 치명 구멍(예전 core_loop_complete 재발 패턴).

## 검증 결과(측정치)
- **신택스**: 19개 프로젝트 55개 JS 전부 ✅ (깨진 코드 0).
- **p1 게이트**: 계측 계약 위반(`auto_deploy`·`defeat_coach` 화이트리스트 누락) 발견 → analytics-worker.js ALLOWED에 추가 → **재검증 🟢 배포 OK** (emit 29종 ⊆ worker 46종).
- **pity 천장**: game.js 3846(+1)/3853(SSR시 0리셋) 로직 정상 — verify ⚠️는 테스트 하니스 아티팩트, 실버그 아님.
- **wrangler 인증**: 살아있음(OAuth 유효).

## 프로젝트 상태 요약
- **p1 daedalus / p2 my-pantheon**: 라이브·git·활발(07-16). 내 핵심.
- **p3 (companion / kami-echoes)**: 내 소관 아님 — 스킵.
- **p4~p21**: 대부분 07-16 신규 소형 스캐폴드(다른 창들). 신택스 클린, 초기 단계.
- **중복/크루프트**: p6(airpods-artistic + art-research-5hr), p10(stable-fee-app + stable-harvest), p3(companion + kami-echoes), 껍데기(p1-daedalus 4K, p7-native 4K) — 정리 후보(삭제는 비가역·neo 판단).

## 다음 한 걸음
- 워커 배포 승인 나면 라이브 반영 → /stats에서 두 이벤트 카운트 확인.

## ⚠️ 승인 필요
1. **analytics-worker 배포** (`npx wrangler deploy -c wrangler-analytics.toml`) — 리텐션 이벤트 폐기 구멍 라이브 봉인. auto-mode가 y/n 요구.
2. (선택) 중복 디렉토리 정리 방향.
3. (별건) 24/7: `~/.legion/anthropic_key`에 API키 한 줄 → launchctl 이미 무장됨.

---
## 추가 실행 (autonomous, neo "알아서 ㄱㄱ")
- **p1 워커 배포 완료** — Version ee23906a. 라이브검증: auto_deploy/defeat_coach→ok:true, 가짜→bad-event(방어정상).
- **p2 배포게이트 신설** — verify.sh + test/runtime-check.js(vm+DOM목, 세션독립). 부팅+15핵심화면 런타임클린 + 계측계약 OK. p2 최초 게이트. 전부 🟢.
- **크루프트 판단**: p6·p3 2폴더는 중복 아님(다른 목적). p10만 진짜 중복 → 삭제 안 하고 핸드오프로 담당창에 확인 요청(상호작용 도트린).

## 붕어빵틀 게이트 탑재 (autonomous 연속)
- **lib/mini-verify.sh + lib/mini-runtime-check.js 신설** → generate.sh가 생성 앱에 자동 복사+실행.
- 4중 게이트: ①치환누락 {{...}} 유출 ②전JS 신택스 ③런타임크래시(TG+DOM목) ④계측계약(ALLOWED.txt).
- 예제 생성 실증: 게이트가 실제 유출 1건 잡음→수정→🟢. 앞으로 p4~p100 전부 상속.
- 오늘 ARSENAL 교훈("새 미니앱 게이트 기본탑재")을 즉시 실천.
