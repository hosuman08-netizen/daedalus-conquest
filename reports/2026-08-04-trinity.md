# Trinity(CPO) · 2026-08-04 · p1 activation 진단

## 무엇을
p1 우선순위 재판정. 07-29 우선순위(win→gacha 전환 CTA) **폐기**하고 P0를 **첫 세션 전투 진입**으로 교체.
핸드오프: `~/MacAI_Conversations/Jarvis/handoffs/20260804-204724-trinity-to-morpheus-p1-activation.md`

## 왜
`activate = 0, 5일 연속`. 07-29 판단은 "전환이 병목"이었으나 **전환 이전에 진입이 막혀 있음**을 실측으로 확인.
승리 오버레이에 붙인 CTA는 승리하는 유저가 0명이라 관객이 없음.

## 검증 (실측 · 추정 없음)
**analytics 라이브 7d (app=daedalus-conquest):** views 11 총합 · activate 0 (07-29의 3건은 팀 스모크 추정) · share 0
**Playwright 라이브:**
- 웹 루트 → 텔레그램 벽만. 게임 진입 불가 (body 461자)
- `?web` + 연령게이트 → 튜토리얼 → 가챠 SSR → **132초 정지**, `running:false`, `totalWins:0`, JS 에러 0건
- 이벤트: age_confirmed→tutorial_start→tutorial_done→gacha_pull→first_gacha→peak_share_gacha→peak_share_any_ssr → 종료
**코드 확정:**
- 게이트: `index.html:22-35` (TG 외 차단, `?web` 우회)
- share 버튼 중복: `game.js:4192`(kind=first_gacha) + `game.js:4245`(kind=ssr) 둘 다 `#gacha-list` 마운트. 중복가드가 kind별 id라 미차단. `game.js:4244` 주석의 전제가 틀림.

## 다음 (한 걸음)
Morpheus: P0-1 SSR 모달 → 전투 자동 진입 · P0-2 share 버튼 1개로 축소.
검증: `?web` 신규 세션 90초 내 `battle_win` 발화 + 스크린샷. 통과 전 완료 주장 금지.

## ⚠️ 승인 필요
1. **웹 게이트 A/B** — (A) TG 전용 유지 → 시딩 링크 전부 t.me로 / (B) 웹 플레이 허용, 결제만 TG. 미결정 시 유입 작업 전부 낭비.
2. Pages `git push` / 배포

## 갭 (띄우기 금지)
- 실유저 트래픽 사실상 0 (일 1~2 view, 봇 가능성 포함). 시딩 이전 단계.
- TG 봇(@daedalus_conquest_bot) 200 응답 확인했으나 **봇 내부 실플레이는 미검증** — 이 맥에서 TG 클라이언트 세션 불가.
- 핸드오프 큐 **고아 9건** (08-02~08-04, RECEIVED 없음) — 자동 브릿지 미가동 의심. 별건.
