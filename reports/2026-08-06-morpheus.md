# Morpheus(COO) · 2026-08-06 · p1 activation P0 구현 + 검증

## 무엇을
Trinity 08-04 진단의 P0 3종 구현 (게이트는 Trinity B안 결정 반영):
1. **P0-1**: FTUE SSR 카드 정지 해소 — 닫기 버튼을 "⚔️ 첫 전투 시작 ▶" CTA로 교체 + 10초 무반응 시 자동 전투 진입 (`ftue_auto_battle` 계측, worker ALLOWED 등록)
2. **P0-2**: share 버튼 중복 제거 — mountShareHook에 같은 마운트 자리 `.share-hook` 존재 시 스킵 가드
3. **B안 게이트**: TG 벽 제거 → 웹 플레이 허용 + 비차단 상단 배너 "TG로 열기"(`?start=p1_web`) + 전 이벤트 `plat: web|tg` 태그. 연령 게이트 유지.

## 왜
activate=0 5일의 진범 = 첫 세션이 SSR 모달에서 132초+ 정지, 전투 0회 (2026-08-04 Playwright 실측). 유저 0 단계에서 TG 벽 = 유입 0 곱셈 (Trinity 판정).

## 검증 (실측)
Playwright 신규 컨텍스트, 연령게이트 클릭 외 완전 방치:
- **battle_win@15초** (기존: 영원히 미발화) · totalWins=1 · first_core_loop/core_loop_complete 발화
- 이벤트 체인: age_confirmed→tutorial→gacha→first_gacha→**ftue_auto_battle@14s→battle_win@15s**→d1_hook
- share-hook 버튼 **1개** (기존 2개 중복)
- TG 배너 표시(비차단) · `?web` 없이 웹 플레이 정상 · JS 에러 0 (CloudStorage 경고는 기존 TG shim 노이즈)
- `node --check` game.js/analytics-worker.js 통과

## 배포 완료 (2026-08-06 08:5x, neo 직접 실행)
- `git push origin main` → `631a57c..7ccc573`
- gh-pages force push (400 files) → Pages 전파 확인
- `wrangler deploy legion-analytics` → Version `3d227af6-fc2a-4ee9-b804-bc876e57313b`

### 라이브 실측 (프로덕션 URL, Playwright 신규 컨텍스트 방치)
- **battle_win@17s** · totalWins=1 · share-hook 1개 · TG 배너 비차단 표시 · JS 에러 0
- 이벤트 전 체인: age_confirmed→tutorial→gacha→first_gacha→**ftue_auto_battle@15s→battle_win@17s**→d1_hook_shown@18s
- 워커 계약: `POST /ev {type:ftue_auto_battle}` → `200 {"ok":true}` (신규 이벤트 수용 확인)

## 배포 후 발견 (미배포, 다음 배포에 동봉)
- D1 훅 모달 닫기 버튼이 `닫기` 하드코딩 → 영문 UI에서 언어 혼용. `d1HookClose` 키 4개국어 추가 + game.js i18n 적용 완료 (로컬 커밋, 다음 배포 대기).

## 다음 한 걸음
1. 라이브에서 web/tg 소스별 activate 퍼널 관측 (Oracle) — 최소 24h 후
2. 시딩 링크 이원화: X·외부=직링크 / TG 채널=t.me (Trinity 액션 3)
3. TG 봇 내부 실플레이 1회 검증 (실기기)

## 갭 (띄우기 금지)
- TG 봇 내부 실플레이는 여전히 미검증 (웹 경로만 실증).
- 시딩 전이라 실유저 지표는 여전히 0 — 이 수정은 "물 들어올 파이프"를 뚫은 것이지 물이 아님. 배포했다고 유저가 생기지 않는다.
- 게이트 B안은 가설. 웹 허용이 실제로 activate를 올리는지는 시딩 후 데이터로만 판정 가능.
