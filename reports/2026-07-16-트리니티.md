# 2026-07-16 트리니티(CPO) — p2 라이브 push GO/NOGO 판정

## 무엇을
p2(My Pantheon) 커밋 c4d078a push GO/NOGO를 CPO 관점(첫인상·전환·리스크)에서 판정. 모피어스 주장을 직접 재검증.

## 왜
push = GitHub Pages 라이브 = 비가역 외부 행동. Sovereign 승인 전 제품 결함 스크리닝.

## 검증 결과 (측정치)
- verify.sh 직접 재실행 → 🟢 전부 GREEN (신택스/런타임/계측계약) — 모피어스 보고 사실.
- 배선 실체 확인: startapp=c-tg-r-(script.js:1359), reserveFounder(:1223), ageGate+youthMode 결제버튼 숨김(:1248,1249), 첫결제 2× 훅(:2239), 인도 RMG 공개문(:2247) 전부 존재.
- **치명 결함 1건 발견 (라이브 E2E)**: `curl legion-pay.../invoice?item=p2_featured` → `{"error":"bad item"}`. 결제 워커에 p2 아이템 미등록 → 유저가 Buy 눌러도 인보이스 안 뜸. 실패 UX는 graceful("Payments are being set up") — 크래시 아님. 근거: script.js:1183 (P2_PAY_BACKEND=""), :1289 (legion-pay 폴백).

## 판정
**GO (조건부)** — push 자체는 저위험(유저 0, BotFather 문 미개방). 단 **시딩 시작 전 결제 워커에 p2_featured 등록 필수** (프론트 push와 독립, 워커측 수정).

## 다음 한 걸음
① Sovereign push y/n → ② 모피어스: legion-pay 워커에 p2_featured(XTR) 아이템 등록 + curl 재검증 → ③ 그 후에만 시딩 개시.

## ⚠️승인 필요 사항
- git push (비가역) — Sovereign y/n.
